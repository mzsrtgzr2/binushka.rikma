_site
vendor
node_modules
.jekyll-cache
.jekyll-metadata
.sass-cache

# Vercel turns every uploaded file under api/ into a Serverless Function, and
# Hobby deployments cap out at 12. Handlers live in lib/routes/ and api/ holds
# only the catch-all plus generated JSON snapshots. Tests live under test/.
api/*.test.js
