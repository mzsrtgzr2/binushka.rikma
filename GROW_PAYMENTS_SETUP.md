# הכנה לאישור סליקה מול Grow (Meshulam) + תוכנית אינטגרציה

מסמך זה מסכם את מה שנדרש מ-Grow (לשעבר "משולם") כדי לאשר את האתר לסליקה, מה תוקן ב-branch הזה, ומה התוכנית הטכנית להמשך (עגלת קניות + קריאות API דרך Vercel).

## 1. רקע - תהליך האישור מול Grow

לפי [תנאי השימוש של Grow](https://grow.business/terms/) (סעיף 4.4):

- פתיחת חשבון ב-Grow **אינה** מספיקה כדי לסלוק באתר. נדרש **אישור סליקה ייעודי לכתובת האתר הספציפית** (domain), בנוסף לאישור פתיחת החשבון.
- לצורך מתן האישור, Grow בודקת את אתר המסחר ודורשת התאמות בכתב (זה בדיוק המייל שקיבלנו).
- אסור לבצע סליקה באתר לפני קבלת אישור הסליקה הייעודי.
- לכל כתובת אתר/דומיין נוסף יידרש אישור נפרד.

## 2. תוצאות הבדיקה שהתקבלה (סבב נוכחי)

אתר שנבדק: `https://rikma.binushka.com`, שם בית עסק: "בינושקה פותחת שולחן".

| סעיף | סטטוס לפני התיקון | טופל ב-branch זה? |
| --- | --- | --- |
| אתר פעיל | ✔ | - |
| טלפון ליצירת קשר | ✔ | - |
| קיים תקנון באתר | ✔ | - |
| הגבלת גיל בתקנון | ✔ | - |
| ביטול עסקה | ✔ | - |
| פרטיות | ✔ | - |
| **כתובת בית עסק** | ❌ | ✅ נוספה כתובת מלאה (אייזנברג 39, רחובות) לעמוד "צרו קשר" ולתקנון האתר |
| **קיים עמוד תשלום** | ❌ | ✅ נוצר עמוד `/checkout/` עם טופס פרטים אישיים (ראו סעיף 3) |
| **אישור תקנון בדף תשלום** | ❌ | ✅ נוסף checkbox חובה בעמוד `/checkout/` |
| **לינק לתקנון בדף תשלום** | ❌ | ✅ ה-checkbox מכיל לינק לתקנון האתר (`/regulations/`) |
| **מדיניות אספקת מוצרים** | ❌ | ⚠️ קיים כבר תוכן מפורט בעמוד [`/shipping/`](/_pages/shipping.md) ובתקנון - כדאי לוודא מול Grow שהעמוד עצמו נגיש/מקושר היטב לפני שליחה לבדיקה חוזרת |
| **אחריות המוצר** | ❌ | ✅ הורחב הסעיף "אחריות המוצר" בתקנון האתר (`/regulations/`) |

> הערה: מייל התשובה של Grow ציין "כתובת" כשדה חובה בטופס, ובעוד סעיף באותה תשובה (הפירוט המצורף) ציין "מדינה" במקום זאת. בעמוד ה-checkout שנבנה נכללו **גם וגם** (כתובת מלאה + עיר + מדינה) כדי לכסות את שתי הדרישות.

## 3. עמוד ה-checkout שנוסף (`/checkout/`)

נוצר עמוד חדש בנתיב `/checkout/` (`_pages/checkout.html`) העונה על דרישת Grow:

> "אם הצורך הוא בסליקה באתר באמצעות תוסף סליקה או API חייב להיות באתר עמוד שבו הלקוח ממלא את פרטיו האישיים לפני מעבר לתשלום. השדות ההכרחיים: שם פרטי, שם משפחה, טלפון ללא קידומת, כתובת וכתובת מייל."

הטופס כולל:

- שם פרטי, שם משפחה (חובה)
- טלפון ישראלי ללא קידומת בינלאומית, עם ולידציה (חובה)
- כתובת אימייל (חובה)
- כתובת מלאה (רחוב + מספר), עיר, מיקוד ומדינה (חובה, חוץ ממיקוד)
- **checkbox חובה** לאישור תקנון האתר, עם **לינק** לצפייה בתקנון בכרטיסייה חדשה

**חשוב:** העמוד הזה הוא עדיין placeholder מבחינת התשלום עצמו - העגלה האמיתית ואינטגרציית ה-API עם Grow טרם נבנו. שליחת הטופס כרגע רק מוודאת שהשדות תקינים ומציגה הודעת "בקרוב". הקישור הישיר `/checkout/` **אינו** מקושר עדיין מהתפריט הראשי, כדי לא לבלבל לקוחות אמיתיים לפני שהעגלה האמיתית תהיה מוכנה - יש לשתף אותו עם Grow ישירות לבדיקה החוזרת.

## 4. מה נדרש מבחינה טכנית להטמעת ה-API (להמשך)

מתוך [דוקומנטציית המפתחים של Grow](https://developers.grow.business/):

### סביבות

| סביבה | Base URL |
| --- | --- |
| Sandbox (פיתוח/בדיקות) | `https://sandbox.meshulam.co.il/api/light/server/1.0/` |
| Production (חי) | `https://secure.meshulam.co.il/api/light/server/1.0/` |

- מזהי הסביבה (`pageCode`, `userId`, ולעיתים `apiKey`) שונים בין Sandbox ל-Production. יש לוודא שימוש במזהים הנכונים לכל סביבה.
- Bit, Google Pay ו-Apple Pay אין להם סביבת בדיקה ייעודית - עסקאות בהם ב-Sandbox מבוצעות בפועל (חיוב אמיתי). PayBox זמין רק ב-Production.
- מזהי ה-Production מתקבלים **רק** לאחר שGrow בדקה את האינטגרציה (Sandbox) ואישרה שהיא תקינה.

### עקרונות אינטגרציה קריטיים

1. **כל הקריאות ל-API של Grow חייבות להתבצע מצד השרת בלבד** (server-to-server). קריאות מהדפדפן ייחסמו. זו הסיבה שהאינטגרציה העתידית תהיה דרך **Vercel serverless function** (ולא ישירות מהלקוח).
2. הבקשות נשלחות כ-`multipart/form-data` (לא JSON).
3. אימות מתבצע לפי `userId` + `pageCode` (עסק רגיל), או `apiKey` + `userId` של העסק (עבור פלטפורמות/מספר בתי עסק).
4. **`ApproveTransaction`** - לאחר קבלת ה-server update (webhook) מ-Grow על תשלום, השרת שלנו **חייב**:
   - להחזיר HTTP 200 ל-webhook.
   - לבצע ולידציות פנימיות (למשל סכום, מזהה הזמנה).
   - לשלוח בקשת `ApproveTransaction` כדי לסגור את העסקה (לא נדרש בזרימות J4/J5).
5. יש לוודא ולידציה של פרטי הלקוח (שם, טלפון, אימייל) **גם בצד שלנו** לפני שליחת הבקשה ל-Grow, כדי למנוע שגיאות נמנעות (זה כבר קיים באופן בסיסי בטופס `/checkout/`).
6. כתובות `successUrl` / `cancelUrl` / `invoiceNotifyUrl` חייבות להיות HTTPS חיצוניות (לא `localhost`) - מתאים היטב לפריסת Vercel שלנו.

### תרשים זרימה עתידי (עגלת קניות + Vercel + Grow)

1. לקוח בונה עגלת קניות באתר (עתידי - טרם נבנה).
2. לקוח ממלא את פרטיו האישיים בעמוד `/checkout/` (**קיים כבר** ב-branch זה).
3. הדפדפן שולח את פרטי העגלה + פרטי הלקוח ל-Vercel serverless function שלנו (למשל `POST /api/create-payment-link`).
4. הפונקציה ב-Vercel (בצד השרת, עם ה-`x-api-key`/`userId`/`pageCode` השמורים כ-Environment Variables מוצפנים) קוראת ל-`createPaymentLink` של Grow עם פרטי הסכום, המוצרים, ופרטי הלקוח.
5. הלקוח מועבר לדף התשלום המאובטח שמחזירה Grow (`paymentLinkUrl`).
6. לאחר תשלום, Grow שולחת webhook (server update) לכתובת ייעודית אצלנו ב-Vercel.
7. השרת שלנו מאמת את העדכון, מחזיר 200, ושולח `ApproveTransaction`.
8. הלקוח מגיע לעמוד "תודה" (`successUrl`).

### קישורים רלוונטיים בדוקומנטציה

- [Testing Environment (Sandbox)](https://developers.grow.business/reference/testing-environment)
- [Live/Production Environment](https://developers.grow.business/reference/live-environment)
- [Authentication](https://developers.grow.business/reference/authentication)
- [Create Payment Link](https://developers.grow.business/reference/create-payment-link)
- [Webhooks](https://developers.grow.business/docs/webhooks)
- [Growin Wallet SDK / client-side implementation notes](https://developers.grow.business/reference/implement-code-on-client-side)

## 5. Next Steps

1. לשתף עם Grow את הקישור `/checkout/` (בענף/סביבת staging שתפורסם מה-branch הזה) ולבקש בדיקה חוזרת של האתר.
2. לוודא מול Grow שסעיף "מדיניות אספקת מוצרים" ב[`/shipping/`](/_pages/shipping.md) ובתקנון עונה על הדרישה (הוא כבר קיים אך סומן כחסר בסבב הקודם - ייתכן שהעמוד לא היה קיים/מקושר בזמן הבדיקה).
3. לאחר קבלת אישור האתר, לפתוח מול Grow בקשה לקבלת פרטי Sandbox (`userId`, `pageCode`, ובמידת הצורך `apiKey`).
4. לבנות את עגלת הקניות בפועל באתר (state ניהול עגלה, חישוב סכום כולל משלוח).
5. להקים Vercel project/functions לצד השרת של האינטגרציה (`/api/create-payment-link`, `/api/grow-webhook`), עם משתני סביבה מאובטחים לאישורי Grow.
6. לבדוק את מלוא הזרימה מול Sandbox, כולל webhook ו-`ApproveTransaction`.
7. לבקש מ-Grow את פרטי ה-Production ולעדכן את ה-Base URL וההזדהות בהתאם, רק לאחר אישור סופי.
